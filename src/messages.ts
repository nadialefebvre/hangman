import { defineMessages } from "react-intl"

export const scope = "app.start"

export default defineMessages({
  instructions: {
    id: `${scope}.instructions`,
    defaultMessage: "This is the default message",
  },
})
